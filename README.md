# Easy QR code generator, one of many.

Your browser likely does this already, but this one won't add Google's silly dinosaur.

Quick and dirty React app that generates QR codes based on user input text.
